engine-strict=true
# Do not let pnpm self-install the version from package.json's "packageManager"
# field: the nested `pnpm add pnpm@...` re-enters the Island sandbox and stacks
# Landlock rulesets until the kernel rejects it (E2BIG). Use the container's
# pre-installed pnpm instead.
manage-package-manager-versions=false
